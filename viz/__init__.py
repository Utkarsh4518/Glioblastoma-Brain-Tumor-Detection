"""Visualization utilities (mesh extraction, etc.) — decoupled from
training/inference code. Pure post-processing on already-predicted label
volumes; nothing here touches the model or the data pipeline."""
