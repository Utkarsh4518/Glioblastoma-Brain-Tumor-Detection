# Runtime dependencies for the Streamlit inference app (app/streamlit_app.py).
# Cloud platforms (e.g. Streamlit Community Cloud) install this file.
#
# For a local GPU setup, install PyTorch from the CUDA index first (see README);
# an already-installed torch satisfies the line below and is left untouched.
# For training / evaluation / the notebook, use requirements-dev.txt instead.
torch>=2.0
monai>=1.3
nibabel>=5.0
numpy>=1.26
streamlit>=1.30
