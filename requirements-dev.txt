# Full environment for training, evaluation, and the notebook.
# Installs the app runtime (requirements.txt) plus dev/training extras.
#
# GPU: install the CUDA build of PyTorch first (see README), then:
#   pip install -r requirements-dev.txt
-r requirements.txt
torchvision>=0.15
h5py>=3.9
hydra-core>=1.3
omegaconf>=2.3
scikit-learn>=1.3
tensorboard>=2.15
matplotlib>=3.7
Pillow>=10.0
tqdm>=4.65
jupyter>=1.0
ipykernel>=6.25
ipywidgets>=8.0
